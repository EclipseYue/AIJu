services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./backend/data:/app/backend/data
      - ${TEXTBOOK_DATA_DIR:-./data/uploads}:/textbooks:ro
    environment:
      TEXTBOOK_DATA_DIR: /textbooks
