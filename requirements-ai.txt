# Optional heavy AI dependencies for local embedding/rerank experiments.
# Keep these separate so the FastAPI/parser skeleton can run on Python 3.13.
# For FlagEmbedding 1.x, Python 3.11/3.12 is recommended because older tokenizers
# releases may fail to compile on Python 3.13.

sentence-transformers==3.3.1
FlagEmbedding==1.3.3; python_version < "3.13"
