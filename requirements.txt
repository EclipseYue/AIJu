# Core production dependencies — lightweight, no GPU/ML bloat.
# System still works: Chroma → JSON cache fallback, BGE → hash vector fallback.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

# PDF / document parsing
pymupdf==1.25.1
pypdf==5.1.0
markitdown==0.0.1a3
python-docx==1.1.2

# Data / retrieval
rank-bm25==0.2.2
openai==1.59.7
httpx==0.28.1

# Optional: install for ChromaDB + BGE embedding support
# chromadb==0.6.3
# sentence-transformers==3.3.1
# numpy==2.2.6
# pandas==2.2.3
# openpyxl==3.1.5
